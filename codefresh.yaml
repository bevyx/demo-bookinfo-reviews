version: '1.0'
steps:
  build:
    type: build
    image_name: bevyx/demo-bookinfo-reviews
    working_directory: ./
    tag: ${{CF_BRANCH_TAG_NORMALIZED}}
  push:
    type: push
    candidate: ${{build}}
    tag: ${{CF_BRANCH_TAG_NORMALIZED}}
    registry: dockerhub